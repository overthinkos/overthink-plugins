# Changelog — 2026-06

### 2026-06-30 — docs(install-plan, plugin): plugin↔plugin dispatch + host-build (microkernel foundation F10)

F10 adds two reverse legs on ExecutorService: InvokeProvider (a plugin invokes another via the host broker)
+ HostBuild (a plugin requests a host-side build). install-plan skill: the `plugin_dispatch_reverse.go` row —
`InvokeProvider` (resolve + InvokeWithExecutor into a nested broker for OOP targets; the generalization of the
RunHostStep ExternalPlugin arm) + `HostBuild` (`hostBuilders` registry; F10 registers `plugin-binary`,
M13/M14 add kustomize/image). plugin skill: a plugin with a reverse channel can `sdk.Executor.InvokeProvider`
a peer / `.HostBuild` — the shared-capability seam (a SHARED plugin is "a plugin others invoke"); reference
`candy/plugin-example-dispatch`. Docs-only.

### 2026-06-30 — docs(go, plugin): plugin lifecycle phases + bootstrap pre-pass (microkernel foundation F9)

F9 adds ordered plugin lifecycle PHASES + a kernel bootstrap pre-pass. plugin skill: a capability declares
its `Phase` (`sdk.Phase*`: bootstrap→schema→load→build→runtime) over Describe; the BOOTSTRAP phase runs
before config validation (the kernel invokes `OpBootstrap` on the raw config bytes), so migrate/egress can
be plugins; bootstrap plugins are compiled-in only; reference `candy/plugin-example-bootstrap`. go skill: the
LoadUnified description documents `runBootstrapPhase` (before `gateSchemaVersion`) + the `phase` capability
field (`providersInPhase`/`phaseCarrier`/`phaseOfProvider`). Docs-only.

### 2026-06-30 — docs(go, plugin): command compile-in — placement-invisible commands (microkernel foundation F8)

F8 closes the all-six-class compile-in mandate — the COMMAND class gains in-proc compiled-in placement.
plugin skill: a command candy can be COMPILED IN (list it in `compiled_plugins:`); `dispatchCommand` routes
it IN-PROC via `Invoke(OpRun)` while an out-of-process one keeps `syscall.Exec` — author dual-placement
(importable `NewProvider`/`NewMeta` + `Invoke(OpRun)` + `Describe` advertising `command:<word>`, plus a
`cmd/serve` shim, both calling one shared effect; mirror `candy/plugin-example-command`). go skill: the
`provider_command_external.go` row documents `dispatchCommand`/`dispatchInProcCommand` (route by placement;
the dynamic Kong grammar identical for both, only the dispatch transport differs). Docs-only.

### 2026-06-30 — docs(go, plugin): generic host-adapter seam + wired OpValidate (microkernel foundation F7)

F7 = C7 (generic host-adapter registration) + C8 (wire OpValidate). go skill: the credential_plugin.go row
now documents the SHARED host-adapter seam — `connectPluginByWord`/`connectPluginByWordRef` (the one
on-demand connect for a verb word in no plan step), which `vm_plugin_client.go` + `k8s_plugin.go` now route
through (the bespoke `ensureVmPluginConnected` sync.Once + kube's bare `ResolveVerb` deleted, R3). plugin
skill: a `class:kind` may serve a deep `OpValidate` check (declare `Validates=true`) returning
`spec.Diagnostics` beyond the static CUE input-def gate; reference `candy/plugin-example-kind` (rejects
`marker: INVALID`). Docs-only.

### 2026-06-30 — docs(install-plan, plugin): substrate-lifecycle + preresolver over gRPC (microkernel foundation F6)

F6 lets an external deploy-substrate plugin bring its OWN host-side venue lifecycle (PrepareVenue/Start/
Stop/Rebuild/…) + preresolve step, served host→plugin over `Provider.Invoke` Op selectors. install-plan
skill: added the `substrate_lifecycle_grpc.go` row — `grpcSubstrateLifecycle` implements the in-core
`substrateLifecycle` by Invoking the plugin + re-materializing the returned `spec.VenueDescriptor` into a
real executor (the live executor never crosses the wire); `wireDeployPreresolver` (`OpPreresolve`); both
registered idempotently at plugin-load (never shadowing a compiled-in pod/vm/k8s/android hook),
RWMutex-guarded; `externalDeployTarget` unchanged. plugin skill: a `class:deploy` capability declaring
`Lifecycle=true`/`Preresolve=true` gets the wire-backed hooks; reference `candy/plugin-example-lifecycle`;
the channel M4 reuses to externalize pod/vm. Docs-only.

### 2026-06-30 — docs(go, plugin): structural-kind tree-decode channel (microkernel foundation F5)

F5 lets a class:kind plugin decode a STRUCTURAL entity — its `OpLoad` returns a `spec.Deploy` member tree the
host folds into `uf.Bundle` (vs F4's flat `uf.PluginKinds`). plugin skill: a kind decode is FLAT or STRUCTURAL;
a structural kind sets `ProvidedCapability.Structural=true` so `runPluginKind` folds its member tree into
`uf.Bundle` (the same map a builtin pod/group populates, same `validateDeploy`) — the channel M3 reuses to
externalize the seven builtin structural kind decoders; reference `candy/plugin-example-structkind`. go skill:
the `plugin_prescan.go` row documents the F5 flat-vs-structural `runPluginKind` fold + the `structuralKindCarrier`
on the grpc/inproc provider. Docs-only.

### 2026-06-30 — docs(go, plugin): parse-time external-KIND prescan + pre-load connect (microkernel foundation F4)

F4 lets a `kind: <plugin-word>` entity whose plugin is NOT compiled in parse + decode during load. go
skill: the `plugin_prescan.go` row now documents the `ClassKind` prescan arm (`registerDeclaredKind` /
`recognizedKind`) AND the F4 kind connect — `connectDeclaredKindPlugins` at the depth-0 loader hook,
re-entrancy-guarded (`inKindConnectPass`), with `normalizeNodeInto` deferring the not-yet-connected kind
node during the connect's nested re-load and erroring loudly if a declared kind never connects. plugin
skill: a `kind` rides config-LOAD (`OpLoad`), connected at parse by F4 (corrects the prior lumping of
kind into the build/deploy lifecycle); reference `candy/plugin-example-kind`. Docs-only.

### 2026-06-30 — docs(install-plan, plugin): the external (plugin-contributed) install-step KIND (microkernel foundation F3)

F3 lets a `class:step` plugin CONTRIBUTE a first-class install-step KIND, carried opaquely as
`external:<word>` with a plugin-DECLARED StepContract. install-plan skill: the step-kind vocabulary now
distinguishes the CLOSED builtin set from the OPEN `external:<word>` family; added the `externalStep` row
(declared Scope/Venue/Gate + opaque Payload; `compileActOp` ClassStep routing; `stepToView`/`stepFromView`
authoritative-flip; the OPEN DEFAULT ARM in `kit.WalkPlans` + `RunHostStep`; `executeExternalStep` reusing
the `OpExecute` dispatch — R3; the carrier M2 reuses); fixed the "thirteen step kinds" framings. plugin
skill: a `class:step` plugin declares its `StepContract{Scope,Venue,Gate}` over Describe
(`ProvidedCapability.step_contract`), pointer to the IR mechanism + `candy/plugin-example-stepkind`.
Docs-only.

### 2026-06-30 — docs(plugin): the host-coupled check-verb kit is dual-placement via the CheckContextService reverse channel (microkernel foundation F2)

F2 lands the `CheckContextService` reverse channel: a host-coupled check verb (`charly/plugin/kit`) now
runs in EITHER placement — compiled-in (the live `*Runner` as the CheckContext) OR out-of-process (the
CheckContext legs served back over ExecutorService + CheckContextService, with `cc.HTTPDo`/`cc.AddBackground`
over the new service and the scalar legs in the env snapshot). Rewrote the plugin skill's "Authoring a
HOST-COUPLED check-verb candy" section (the kit is no longer COMPILED-IN-ONLY; documents the `cmd/serve` +
`sdk.ServeCheckVerb` out-of-process path and `cc.HTTPDo` replacing `cc.HTTPClient()`); added
`ServeCheckVerb` to the SDK surface list. Docs-only.

### 2026-06-30 — docs(go): document the generic verb-preresolver registry (microkernel foundation F1)

F1 adds `verb_preresolve.go` + `verb_preresolve_wrappers.go` to charly's core: the GENERIC per-verb
host-side preresolver registry (`registerVerbPreresolver` + the opaque `CheckEnv.Substrate
json.RawMessage`), the check-verb analogue of `registerDeployPreresolver` / `DeployVenue.Substrate`.
New go-skill file-map row documenting it; the cdp/vnc/mcp/spice preresolvers now register through the
registry and ship their resolved endpoint via `CheckEnv.Substrate` (the hardcoded
`CheckEnv.{Cdp,Vnc,Mcp,Spice}` typed fields + the fixed five-preresolver dispatch block are gone), and
`kube` registers a wrapper that rewrites the op's resolved cluster context. Docs-only.

### 2026-06-30 — docs(vm-spec, go, cloud-init-renderer, kubernetes, pod, vms-catalog, migrate): repoint deleted dead-stub source files (microkernel cleanup K1)

R5 claim-keyed sweep paired with the superproject's K1 dead-stub deletion. Seven skills named files
that were deleted in `charly/` (`vm_spec.go`, `cloud_init_types.go`, `k8s_spec.go`, `pod_spec.go`) — each
a zero-symbol stub whose types are now generated in `charly/spec/cue_types_gen.go` (or live in
`charly/vmshared/`). The source-file rows / references were repointed to the real homes. Surgical
repoints only; the broader VM source-map staleness (`libvirt_yaml.go` / `qemu_render.go` /
`cloud_init_render.go` → `vmshared`, from the prior go-libvirt shed) folds into the program's `vmshared`
cutover. Docs-only; gated by the non-runtime standards (cross-reference + grep self-test).

### 2026-06-30 — docs(plugin, generate-source): the four detection-builders externalized — R5 claim sweep

R5 claim sweep for the superproject's detection-builder externalization (cargo/npm/pixi/aur are now
external out-of-process plugin candies serving the deploy-time `OpCollectContext`/`OpReverse` legs via
a new builder-IR-over-RPC seam; their build-time multi-stage stays the embedded vocabulary). Corrected
the now-false "in-proc `BuilderProvider`" claims: dropped `BuilderProvider.Reverse` from the
placement-perf-invariant fast-path list (plugin SKILL), and rewrote the `emitExternalBuilderStages`
note (generate-source SKILL) — the four are ALSO external `*grpcProvider`s now but serve only the
deploy-time legs and are SELECTED BY DETECTION, so mis-selecting one via `external_builder:` fails
loudly at the `OpResolve` Invoke. Present-tense; the superproject CHANGELOG/2026-06.md carries the
cutover.

### 2026-06-29 — docs(install-plan, vm-deploy-target, deploy): pod deploy substrate externalized — all five substrates external

R5 claim sweep for the superproject's deploy:pod externalization (the LAST substrate). pod is now an
out-of-process plugin (candy/plugin-deploy-pod serving deploy:pod) via the Design A substrateLifecycle
hook — podSubstrateLifecycle.PrepareVenue builds the overlay HOST-SIDE through the RETAINED core build
engine (PodDeployTarget/OCITarget/Generator), exactly as vm keeps its libvirt build core. Removed
PodUnifiedTarget refs, corrected "four external substrates" → "five (local/vm/pod/k8s/android)", and
documented the pod-now-external + retained-build-engine model. Present-tense; the superproject
CHANGELOG/2026-06.md carries the cutover.

### 2026-06-29 — docs(vm-deploy-target + 14 targeted + android): vm deploy substrate externalized (Design A lifecycle hook)

R5 doc sweep for the superproject's deploy:vm externalization. The in-proc VmDeployTarget / unified_targets_vm.go
were DELETED; vm is now an out-of-process plugin (candy/plugin-deploy-vm serving deploy:vm) running the shared
charly/plugin/kit.WalkPlans over the guest SSHExecutor, with the VM lifecycle (PrepareVenue / Start / Stop /
Status / Logs / Shell / Rebuild / nested-pod-in-guest) a registered host-side `substrateLifecycle` hook.
/charly-internals:vm-deploy-target rewritten (VmDeployTarget is no longer the 4th in-proc DeployTarget — the
in-proc set is OCITarget / Pod; android/k8s/local/vm are external) + 14 targeted refs across the deploy /
install-plan / go surfaces. Also corrected the /charly-check:android skill's renamed-file ref
(deploy_add_cmd.go → bundle_add_cmd.go). Present-tense; the superproject CHANGELOG/2026-06.md carries the cutover.

### 2026-06-29 — docs(install-plan, vm-deploy-target, local-deploy, deploy, go, build, generate, generate-source, local-infra, tailscale-up): local deploy substrate externalized; RunBuildStep→RunHostStep

R5 doc sweep for the superproject's local-externalization cutover: the `local` deploy substrate left charly's
core for candy/plugin-deploy-local (`deploy:local`) via the F1 seam + F2/F3. The in-proc LocalDeployTarget /
LocalUnifiedTarget (+ unified_targets_local.go / plugin_deploy_local.go) were DELETED; the InstallPlan IR is
now consumed out-of-process via charly/plugin/kit.WalkPlans — plugin-renderable kinds via the F2 Executor
legs, the 5 host-engine kinds (Builder / LocalPkgInstall / SystemPackages / act-verb OpStep / ExternalPlugin)
via the generalized **RunHostStep** RPC (renamed from RunBuildStep). Swept LocalDeployTarget / LocalUnifiedTarget
/ RunBuildStep across 10 skills + corrected the in-proc DeployTarget enumeration to OCITarget / Pod / Vm
(in-proc) + external (android / k8s / local). VmDeployTarget / PodDeployTarget / OCITarget kept — still core.
Present-tense; the superproject CHANGELOG/2026-06.md carries the cutover record.

### 2026-06-29 — docs(install-plan, go, kubernetes, vm-deploy-target, build, generate, generate-source): k8s deploy substrate is now an out-of-process plugin

R5 doc sweep for the superproject's `deploy:k8s` externalization cutover: the `k8s` deploy substrate left
charly's core for `candy/plugin-kube` (`deploy:k8s` beside `verb:kube`) via the F1 substrate-kind-plugin
dispatch seam. The in-proc `K8sDeployTarget` / `K8sUnifiedTarget` were deleted; there is no in-proc k8s
DeployTarget. The Kustomize GENERATOR (`GenerateK8sKustomize`, `charly/k8s_generate.go`) STAYS in core
(egress-gated; second consumer `charly bundle from-box --target k8s`): the host-side `deploy:k8s` preresolver
(`charly/k8s_deploy_preresolve.go`) generates the egress-validated tree and ships the overlay path in
`DeployVenue.Substrate`; the plugin runs `kubectl --context <ctx> apply -k`. Updated `/charly-internals:install-plan`
(removed `K8sDeployTarget`/`k8s_target.go`; "Five implementations" now = OCITarget/Pod/Local/Vm/external),
`/charly-internals:go` (the Kubernetes-substrate + IR-flow sections), `/charly-kubernetes:kubernetes` (the
Relevant-code map), `/charly-internals:vm-deploy-target` (the DeployTarget ordering), and the `build` /
`generate` / `generate-source` IR-consumer enumerations. Present-tense only; the superproject
`CHANGELOG/2026-06.md` carries the cutover record.

### 2026-06-29 — docs(/charly-check:android, /charly-core:deploy): android deploy substrate is now an out-of-process plugin

R5 doc sweep for the superproject's `deploy:android` externalization cutover: the `android` deploy
substrate left charly's core for `candy/plugin-adb` (`deploy:android` beside `verb:adb`) via the new F1
substrate-kind-plugin dispatch seam. Updated `/charly-check:android` and `/charly-core:deploy` to describe
the substrate as plugin-served (the in-proc `AndroidDeployTarget` + `plugin_deploy_android_reg.go` and the
android install machinery were deleted from core), with the general per-substrate deploy pre-resolver hook
feeding the resolved adb device endpoint + apk install specs over the wire. Present-tense only; the
superproject `CHANGELOG/2026-06.md` carries the cutover record.

### 2026-06-25 — docs(/charly-internals:plugin): the host-engine kit — authoring a host-coupled check-verb candy

New "Authoring a HOST-COUPLED check-verb candy (the kit, COMPILED-IN-ONLY)" section: a check verb needing
the live `*Runner` implements `charly/plugin/kit.CheckVerbProvider` (`RunVerb` on `kit.CheckContext`),
compiled-in via `registerCompiledCheckVerb` (the `kitVerbAdapter` + `runnerCheckContext`), with pluginsgen
detecting the kit shape (exported `NewCheckVerb`). `candy/plugin-port` is the reference; documents the
progression from an in-charly-module verb to a kit candy (the verb's Go leaves charly's module, keeping the
in-proc fast path).

### 2026-06-25 — docs(/charly-internals:plugin): exampleprobe relocated to a candy — retire the deleted-builtin references

The "Authoring a BUILTIN plugin" section mirrored `charly/plugin/builtins/exampleprobe` + `plugin_example.go`,
both now DELETED (exampleprobe relocated to `candy/plugin-example`, a dual-placement candy module). Re-point
the section at the remaining in-charly-module example (`examplerunverb` / `plugin_examplerunverb.go`), note
the in-charly-module path is being RETIRED as builtins relocate to candies, and that the `source: builtin`
candy-linkage form is gone (R1/R5 — the section's deleted-path references were stale).

### 2026-06-25 — docs(/charly-internals:plugin): compiled-in plugin candies (compiled_plugins + go.work) + fix the false "no go.work" claim

Documents the new in-proc placement of a candy: `charly.yml` `compiled_plugins:` + `pluginsgen` →
`go.work` + `plugins_generated.go` + `InProcServedTransport`/`registerCompiledPlugin` + the coexist switch
(new "Authoring a COMPILED-IN plugin candy" section, with the perf note: a compiled-in candy dispatches the
in-proc `Invoke` envelope, a legacy in-charly-module builtin uses its typed fast path). Fixes the now-false
"a builtin's Go lives in charly's module (no `go.work` — charly cannot import `candy/`)" claim (R1/R5): a
`go.work` exists and charly imports `candy/` for compiled-in plugins; the in-charly-module path is the LEGACY
builtin path. Notes `buildPluginBinary` `GOWORK=off` on the external-authoring section.

### 2026-06-23 — docs: the core charly is cgo-free for SPICE audio — drop the stale "needs libportaudio" claim (spice dep-shed follow-up)

The spice dep-shed moved the opus/portaudio cgo audio code out of core into candy/plugin-spice (gated behind
`-tags spice_audio`); the core charly links no libportaudio/libopus (`ldd` confirmed). Reframe the now-false
"the host charly binary is built with cgo for SPICE audio / needs libportaudio.so.2 / exits 127 without it"
claims (R5 claim-keyed): `/charly-check:libvirt` (the "Cloud-image VM packages: also include portaudio" section
+ the exit-127 paragraph) and `/charly-vm:arch` (the cloud_init "SPICE-audio .so deps" mirror) now state the
cgo-free reality — SPICE audio is an opt-in `-tags spice_audio` build of candy/plugin-spice.

### 2026-06-23 — docs: the `spice` verb is externalized — reframe the spice/check/libvirt/arch/ssh skills to the declarative-verb reality (R5 claim-keyed sweep)

The `spice` SPICE-wire check verb was dep-shed into the out-of-process plugin `candy/plugin-spice`
(Shells-com/spice + opus + portaudio left charly's core go.mod); per charly/check_cmd.go there is NO host
`charly check spice` CLI subcommand — it exists only as the declarative `spice:` check verb (the host
pre-resolves the VM's live SPICE endpoint via vm_target.go, then the plugin speaks the wire). Reframe the
false host-CLI framing across the sibling surfaces (R5 claim-keyed):

- `/charly-check:spice`: Overview + the "Command surface" reframed to the declarative `spice: <method>` step
  form (method = `spice:` value; x/y/text/key/artifact as sibling modifiers); the remote-libvirt section to
  the verb + `CHARLY_LIBVIRT_URI`; Implementation/Dependencies rewritten for candy/plugin-spice (the verb +
  Shells-com/spice + the vendored third_party/spice + the spice_audio-gated opus/portaudio) + the host-side
  `preresolveSpiceEndpoint`. Every method semantic + the scancode/native-screenshot/qemu+ssh-tunnel notes +
  the spice-vs-libvirt architecture split preserved.
- `/charly-check:libvirt`, `/charly-vm:arch`, `/charly-core:ssh`: the `charly check spice …` cross-references
  reframed to the `spice:` verb / `CHARLY_LIBVIRT_URI` (libvirt's own host-CLI framing kept — still a CLI
  command). `/charly-check:check`: spice moved from the in-core probe-verb list to the out-of-process group
  (8→7), the subcommand-table row + reserved-image-name entry removed.
- `/charly-internals:go`: the check_cmd.go source-map corrected (7 in-core live-container sub-Cmds, not 9;
  kube/adb/appium/spice are out-of-process plugin verbs, not sub-Cmds).

Paired with the superproject's spice dep-shed cutover (same date).

The `kube` cluster-probe verb was dep-shed into the out-of-process plugin `candy/plugin-kube` (client-go +
apimachinery left charly's core go.mod); per charly/check_cmd.go there is NO host `charly check kube` CLI
subcommand — it exists only as the declarative `kube:` check verb. Reframe the false host-CLI framing across
the sibling surfaces (R5 claim-keyed):

- `/charly-kubernetes:check-k8s`: Overview + the "Command surface" reframed to the declarative `kube: <method>`
  step form; the "Cluster selection" precedence kept but presented as the `cluster:`/`kubeconfig:`/`kube_context:`
  modifiers, with the stale `LoadClusterProfile` reference (removed in schema v4) corrected to `findK8sSpec` +
  `preresolveKubeCluster`; the Implementation section rewritten for candy/plugin-kube + the registry dispatch +
  `charly/k8s_plugin.go` (invokeKubePlugin / merge-kubeconfig). Every method semantic + the apply/delete/raw/
  addons/lb-external-ip notes preserved.
- `/charly-kubernetes:kubernetes`: unchanged (K8sDeployTarget / cluster-profiles / Kustomize stay in core).
- `/charly-infrastructure:k3s-agent`, `/charly-infrastructure:k3s-server`: the runnable `charly check kube …`
  quickstart lines reframed to the declarative `kube:` step; the verb-not-host-CLI framing applied.
- `/charly-check:check`: the in-core "live-container probe verbs" list dropped `kube` (it is now an
  out-of-process plugin verb, parallel to adb/appium); a pipe-form `…|kube` host-CLI claim also corrected.
- `plugins/README.md`: the plugin-index kube row reframed.

Paired with the superproject's kube dep-shed cutover (same date).

The `adb` and `appium` verbs were dep-shed into out-of-process plugin modules (`candy/plugin-adb`,
`candy/plugin-appium`); per `charly/check_cmd.go` "there is no host `charly check adb`/`appium`" — they exist
ONLY as the declarative `adb:` / `appium:` check verbs. The `/charly-check:adb` and `/charly-check:appium`
skills still framed every method as a runnable `charly check <verb> <method> <image>` host CLI command (the
appium skill carried this from the c01040e8 gap; the adb cutover surfaced the same claim) — now false.

- `/charly-check:adb`, `/charly-check:appium`: Overview + Quick Reference reframed so the declarative verb is
  the primary (and only) form — the "CLI form" column → declarative `<verb>: <method>` authoring + sibling
  modifier fields; `--flag` references converted to their declarative field names. Implementation sections
  point at the out-of-tree plugin module + the registry dispatch (`ResolveVerb` → grpcProvider →
  `invokeVerbProvider`; the adb deploy/status seams via `charly/android_plugin.go`). Every method semantic,
  modifier table, and gotcha preserved — a framing fix, not a content cull.
- `/charly-check:android`: the three "host CLI" mentions reframed to the `adb:`/`appium:` declarative verbs.

Paired with the superproject's adb dep-shed cutover (same date).

The `/charly-check:check` skill's "a step verb MUST NEVER be spelled like a reserved KIND word" list still
named `host` — but cutover C removed the `host:` venue kind (a host deploy is the `host:` FIELD on `local:`).
Drop it from the list. Documentation-only.

### 2026-06-21 — docs: EDGE-INHERIT cutover D — the `box:` kind merged into `candy:`, in the skills

Sweep cutover D across CLAUDE.md + the skills (93 SKILL.md) + add strict MUST-form authoring guidance: the
`box:` KIND is GONE — an IMAGE is a `candy:` node carrying `base:` (an external base) or `from:` (a builder
ref, the former box:); a `candy:` with neither is a LAYER fragment. Every `box:` composition-example
discriminator → `candy:`; the image-vs-layer authoring rule is stated as a MUST in `/charly-image:image` +
`/charly-image:layer` (the canonical owners). The `charly box` COMMAND family, the `box/<name>/` discovery
directory, and the internal `BoxConfig`/`uf.Box` Go names are KEPT (a command/dir/type, not the YAML kind) —
only the `box:` kind keyword changes. The migrate skill table gains the `box-to-candy` step row and the new
HEAD `2026.172.0006`. Documentation-only change class.

### 2026-06-21 — docs: EDGE-INHERIT cutover C — `host:` venue kind removed, `group`→`package-group`, in the skills

Sweep cutover C across the skills + add strict MUST-form authoring guidance: the `host:` venue KIND is GONE
(a host/remote deploy MUST be the `host:` FIELD on a `local:` deploy — `local: {from: T, host: <user@machine>}`,
never a standalone `host:` node); the Calamares package group MUST use `package-group:`, so `group:` is the
targetless DEPLOY group ONLY. Touched: CLAUDE.md Key Rules + `/charly-core:deploy`, `/charly-local:local-deploy`,
`/charly-image:layer`, `/charly-pod:pod`. ALSO backfills `/charly-build:migrate`'s step table with the
cutover-A (`probe-verb-rename`) + cutover-B (`edge-inherit`) rows and corrects the calver-schema HEAD to
`2026.172.0004` (a stale-since-cutover-A doc gap). Documentation-only.

### 2026-06-21 — docs: EDGE-INHERIT cutover B — `bundle:` kind → substrate kinds at the edge in the skills

Sweep the superproject's EDGE-INHERIT cutover B (the `bundle:` deploy KIND is eliminated — a deploy carries
its substrate kind `pod:` / `vm:` / `k8s:` / `local:` / `android:` / `group:` directly at the EDGE, with
`image:` = the box/OCI image a `pod`/`k8s`/`android` runs and `from:` = inherit a same-kind template by name)
across the affected skills: CLAUDE.md's Key Rules + the ~27 `SKILL.md` files that document deploys (heaviest:
`/charly-core:deploy`, `/charly-image:image`, `/charly-pod:pod`, `/charly-kubernetes:check-k8s`,
`/charly-local:local-deploy` + `local-spec`, `/charly-check:check`, `/charly-build:validate`,
`/charly-internals:install-plan` + `disposable`). Every `bundle:{box:X}` YAML example → `pod:{image:X}`,
`bundle:{vm:V}`→`vm:{from:V}`, a targetless `bundle:`→`group:`, "a disposable bundle"→"a disposable deploy".
The `charly bundle` command + the `BundleNode`/`BundleConfig` Go type names are UNCHANGED. Documentation-only.

### 2026-06-21 — docs: EDGE-INHERIT cutover A — `charly check k8s`/`group` step verbs → `kube`/`unix_group` in the skills

Sweep the superproject's EDGE-INHERIT cutover A (step probe verbs `k8s`/`group` collided with the `k8s`
deploy kind + the Calamares `group` kind; renamed to `kube`/`unix_group`) across the affected skills:
`/charly-check:check` (verb catalog: `group`→`unix_group`, `k8s`→`kube` rows + a new STRICT-RULE block
"a step verb or Op modifier MUST NEVER be spelled like a reserved kind word", machine-enforced by
`spec/TestNoKindWordAsStepVerb` + `TestNoKindWordAsOpModifier`), `/charly-kubernetes:check-k8s` +
`/charly-infrastructure:k3s-server`/`k3s-agent` (`charly check k8s`→`charly check kube`, `k8s:`/`k8s_*:`
step examples → `kube:`/`kube_*:`), and `README.md`. The `k8s` deploy KIND, `--target k8s`, the
`/charly-kubernetes:check-k8s` skill NAME, and the `check-k3s-vm` bed are unchanged.

### 2026-06-21 — docs(plugin): new `/charly-internals:plugin` skill for the unified plugin-schema model

New topic skill `/charly-internals:plugin` documents the plugin authoring contract main's unified-schema
cutover establishes: a plugin is a candy with a `plugin:` block; the unified Provider model
(transport-invisible); the per-plugin `.cue` schema as the SINGLE SOURCE used two ways (`gengotypes` for
dev params + schema-over-`Describe` RPC for runtime validation, `base ++ plugin`); the ZERO
builtin/external distinction (`PluginUnit` via `InProcTransport`/`LocalTransport`, one load gate + one
validator); how to author a built-in (in charly's module, `charly/plugin/builtins/<name>/`) vs an external
(its own module + the SDK `BuildCapabilities`); and the self-contained-schema contract. The CLAUDE.md Skill
Dispatcher gains a plugin-authoring row (superproject), and `/charly-check:check` cross-references the
`check: { plugin: <verb> }` step's runtime schema validation.

---

### 2026-06-21 — docs(generate-source): fix the stale VM-era OCI-label table (R1 divergence)

The `/charly-internals:generate-source` OCI-label table listed 7 labels that no longer exist in `charly/labels.go` (claim-keyed R1 sweep — confirmed this skill was the only carrier). Corrected to current reality:
- **Removed** (gone from `labels.go`): `ai.opencharly.vm` + `ai.opencharly.libvirt` (VM moved to `kind: vm`), `ai.opencharly.fqdn` + `ai.opencharly.acme_email` + `ai.opencharly.engine` (tunnel + engine are deploy-`charly.yml`-only now, consistent with the table's own "tunnel is NOT an OCI label" note).
- **Renamed** (layer→candy rebrand): `ai.opencharly.env_layer` → `ai.opencharly.env_candy`, `ai.opencharly.layer_version` → `ai.opencharly.candy_version`.

Prose-only.

---

### 2026-06-21 — docs(skills): the init system is now an OCI-label single-source (`ai.opencharly.init_def`)

Three skills updated for the init-def-label cutover (superproject): the build-resolved init contract (entrypoint + fallback + management tool/commands) is now baked into a new `ai.opencharly.init_def` OCI label and read at deploy LABEL-FIRST, so the hardcoded `wellKnownInitDefs` registry is only the legacy fallback for pre-label images, and custom init systems declared in the embedded vocab now work at RUNTIME (previously build-only).

- **`/charly-internals:capabilities`** — added `InitDef` → `LabelInitDef` (`ai.opencharly.init_def`) to the key-labels table (the `CapabilityInitDef` JSON subset: entrypoint / fallback_entrypoint / management_tool / management_commands).
- **`/charly-core:service`** — the entrypoint + service-management commands are sourced from the label (the "custom init honored at build only" caveat removed); `wellKnownInitDefs` is the legacy fallback.
- **`/charly-internals:generate-source`** — the OCI-label table notes `ai.opencharly.init_def` and the label-first deploy read.

Prose-only.

---

### 2026-06-20 — docs(migrate): note that unified-node/step-venue convert the per-host deploy overlay + the new install-strategy-key step

`/charly-build:migrate` migration-chain table: the `unified-node` (2026.169.0001) and `step-venue` (2026.169.0003) rows now state they ALSO run the node-form transform over the per-host deploy overlay (`ctx.HostDeployPath` — the runtime `~/.config/charly/charly.yml`) via `migrateHostOverlayDoc`, not just project files (else the overlay's legacy top-level `deploy:` map stays HEAD-stamped yet loader-rejected with no recovery path). Self-gated on a non-empty `HostDeployPath` so the project-only / remote-cache runner still skips host state (`TouchesHost` stays false). Adds the new `install-strategy-key` (2026.169.0002) row — the `ov_install_strategy` → `charly_install_strategy` vm_state key rename — so the table stays in lockstep with the registry. Prose-only.

---

### 2026-06-20 — docs(skills): reverse the do-NOT-use-gengotypes guidance + document the CUE-single-source `spec` package

The superproject's CUE-single-source cutover makes `charly/schema/*.cue` the sole author-of-record for the `charly.yml` ingress grammar and GENERATES the Go param structs (`cue exp gengotypes`, `task cue:gen`) + the reserved-word vocabulary into the new `charly/spec` package. Six skills are swept to the new model:

- **`/charly-internals:go`** — point-5's "Do NOT generate the Go structs with `cue exp gengotypes`" rule (it generalized disjunctions to `any` and remapped names; keep the hand lockstep) is REVERSED. A rewritten "CUE is the single source of truth — the `charly/spec` package" section documents `task cue:gen`, the generated `cue_types_gen.go` / `vocab_gen.go`, the hand-written union/alias/state companion files (`union_types.go` / `scalar_aliases.go` / `hand_state_types.go` — the `@go(-)` disjunction-precision escape), the `gen_repro_test.go` reproducibility gate, the `charly/internal/schemagen` + `charly/internal/schemaconcat` generators, the `spec_aliases.go` compile-time parity gate, and the `reserved_registry.go` reserved-word⇄handler bijection registry. The loader narrative now states `#NodeDoc` is the SOLE load-time gate (legacy kind-keyed / root-shape routing hard-rejected with a `charly migrate` hint; `mergeKindDoc` / `firstKindKey` / `VmDoc` and the eight hand vocab lists deleted in favor of the CUE-derived `kindWordSet` / `resourceKindSet` / `stepKeywordSet` / `dataKeySet` / `docDirectiveSet`).
- **`/charly-build:validate`** — the param schema is now CUE-single-source: changing a validated field is a CUE edit → `task cue:gen`, pointing at the go skill's "Updating Go code when an ingress CUE schema changes" recipe; authoring is node-form only (a legacy kind-keyed document is rejected with a `charly migrate` hint).
- **`/charly-internals:egress`** — the egress schema generation now rides the same `task cue:gen` / `gengotypes` reproducibility gate, and the `sharedCueSchema` concat note aligns with the shared `schemaconcat` contract.
- **`/charly-image:image`, `/charly-image:layer`** — the box/candy/op kind schemas they reference are now CUE-single-source (`charly/schema/*.cue` → `charly/spec` via `task cue:gen`); changing a box/candy/plan-step field is a CUE edit, pointing at the go-skill recipe.
- **`/charly-build:migrate`** — states the validated schema is CUE-single-source and that a pure codegen refactor leaving every authored wire key untouched needs NO migration step and NO `version:` bump — exactly this cutover (HEAD stays `2026.169.0004`).

No authored wire key changed, so the superproject ships no migration step. Prose-only.

---

### 2026-06-20 — docs(selkies): add missing frontmatter to 6 skills so they register (`v2026.171.0758`)

Six `selkies` skills shipped without YAML frontmatter — `a11y-tools`, `wl-tools`, `wl-screenshot-grim`, `waybar-labwc`, `xdg-portal`, `xterm` — so the skill loader could not register or dispatch them (`/charly-selkies:*` was silently missing them). Added a `name:` (the skill dir-name) + a `description:` `|`-block (a what-it-is line + a "Use when…" hint) to each, matching the convention of the 282 valid sibling skills; bodies untouched (additions-only). A full-tree scan now reports 288/288 SKILL.md with valid frontmatter (was 282/288). Documentation-only.

---

### 2026-06-20 — docs(skills): reinforce plan-as-immutable-contract + delete transitional code before the R10 acceptance run (`v2026.171.0618`)

Sharpens two existing mandates in the hard-cutover skills so neither can drift. `cutover-policy`: the transitional-code rule now deletes transitional / legacy-accepting / dual-dispatch paths BEFORE the R10 *acceptance run* (not merely "before the end" / before commit) — a transitional path still present when the acceptance run executes means R10 verified a non-shipping state and the deletion then rides untested into the commit (the R5 silent-skip regression); mid-flight smoke/RDD runs with transitional code present stay encouraged (they never authorize the commit). The contract paragraph adds the symmetric immutability: an approved plan is never narrowed, widened, re-approached, or rewritten mid-execution — the only legal change is STOP-and-ask. `strict-policy` R5: the acceptance test gains the same before-the-acceptance-run timing, and the R1–R5↔R6–R10 section states R5's deletion is a PRECONDITION of R10's acceptance run. The mandate stays single-sourced in CLAUDE.md (these skills carry the detail). Prose-only.

---

### 2026-06-19 — docs(skills): sync the migrate registry table to schema HEAD 2026.169.0004 (`v2026.170.2045`)

The migrate skill's migration-chain table + its HEAD-version references were stuck at the superseded `2026.165.1048` (the then-`calver-schema` HEAD). Added the 3 steps the code carries past it — `unified-node` (2026.169.0001, the unified name-first node migration), `step-venue` (2026.169.0003, venue-from-tree-position; the `pod:`/`on:` step-venue overrides retired, `${PEER_*}`→`${HOST:…}`), and the re-stamped `calver-schema` HEAD (**2026.169.0004** = `latestSchemaVersion`) — and fixed the two stale HEAD-version examples (the schema-version prose + the load-gate error message). Surfaced while fixing the per-kind-sibling claim in the same skill. Prose-only.

---

### 2026-06-19 — docs(skills): document requires_shared in the deploy skill + fix stale per-kind-sibling claims (`v2026.170.2037`)

The deploy skill's resource-arbitration coverage lost `requires_shared` in the unified-node rewrite — re-added it (the arbitration bullet + the "Preemptible resource arbitration" section title + a dedicated bullet): the REFCOUNTED shared-claimant (a GPU-CDI pod's `requires_shared: [nvidia-gpu]` flips the whole IOMMU group vfio→nvidia+CDI at the first claimant's deploy, back nvidia→vfio when the last releases), mutually exclusive with `requires_exclusive:` per `validate_preempt.go` — a real `DeploymentNode` field (`deploy.go:337`). Separately, the migrate + deploy skills present-tense-claimed `charly.yml` carries "the per-kind siblings `box.yml`/`deploy.yml`/`vm.yml`/…" as current versioned files; the single-filename migration (2026.160.1300) folded those into `charly.yml`, so per-kind splits are now an optional `import:` convenience (the image skill's accurate framing) — corrected both. Prose-only.

---

### 2026-06-19 — docs(coder): align the ubuntu-coder bootstrap-comment example with the embedded model (`v2026.170.1900`)

`ubuntu-coder/SKILL.md:83`'s transcribed generator bootstrap comment ("declared in build.yml distro.base_user") updates to "declared in the embedded distro.base_user" — matching the corrected `generate.go` emitted string (the `build.yml` distro/builder/init vocabulary is embedded at `charly/charly.yml`). Lands with the superproject `charly/*.go` build.yml comment sweep (`v2026.170.1900`). Prose-only.

---

### 2026-06-19 — docs(skills): sweep stale build.yml/charly.cue refs to the embedded charly.yml model (`v2026.170.1736`)

63 SKILL.md files rewrote current-state references to the dropped `build.yml` and reverted `charly.cue`/`cue_source.go` to the post-§3 model: the distro/builder/init/resource/sidecar build vocabulary is EMBEDDED in the `charly` binary at `charly/charly.yml` (renamed from `charly_defaults.yml`). Migration-registry history rows, the `build.yml` flat-import OVERRIDE note, and Go-source filenames kept verbatim. Comment-/prose-only; orchestrated via the `build-yml-embed-doc-sweep` workflow (one agent per file).

---

### 2026-06-19 — docs(skills): align skills with the unified name-first node model (`v2026.170.1105`)

Skills rewritten for the unified `<name>: {<kind>: …, <children>}` node model:
`deploy:`→`bundle:` / `charly deploy`→`charly bundle`, venue + membership from
tree position (the cross-deployment-probing section rewritten to node-form group
beds with `${HOST:<member>}` superseding `${PEER_*}`), the `agent_provisioned:`
member flag documented (check skill), `DeploymentNode`→`BundleNode` throughout.
See the superproject `CHANGELOG/` for the full unified-node-bundle change.

---

### 2026-06-17 — docs(skills): hooks-doctrine amendment + per-repo monthly `CHANGELOG/` model

Adopted this repo's own `CHANGELOG/` (one file per month). Amended the "Hooks
doctrine" in `/charly-internals:agents` so the prompt-submit reminder names the
FULL R0-R10 + RDD + ADE roster as second-pass *triggers* (the "≤10 output lines /
one behavioral anchor" caps are lifted) while keeping the anti-duplication core —
reminders never restate rule BODIES; CLAUDE.md is the single source. Added the
new runtime-tier per-repo CHANGELOG-entry gate to the enumerated PreToolUse
invariants. Swept the CHANGELOG-model wording (history -> each repo's
`CHANGELOG/`, monthly files) across the skill set (skills, cutover-policy,
strict-policy, git-workflow, go-quality, build, merge, migrate, disposable) +
`plugins/README.md` + `plugins/CLAUDE.md`. Documentation-only.
