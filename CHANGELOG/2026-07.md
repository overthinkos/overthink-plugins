# Changelog — 2026-07

### 2026-07-01 — docs(enc): document proactive user_allow_other handling (C16a follow-up)

`automation/skills/enc/SKILL.md`: new "Host prerequisite: user_allow_other in /etc/fuse.conf" note under
"Why -allow_other is Required" — charly now checks it proactively (`charly doctor` Encrypted Storage group
via `checkFuseAllowOther`, and the in-core enc shim preflights it via `fuseAllowOtherEnabled` before a
mount/ensure op, failing fast with the exact fix instead of the raw fusermount3 error).

### 2026-07-01 — docs(go, enc): gocryptfs mount mechanics externalized to candy/plugin-enc (C16a)

Paired with the superproject's C16a cutover. The gocryptfs SHELLING moved out of charly core into the
compiled-in `candy/plugin-enc` (`verb:enc`); the in-core `charly/enc.go` is now a thin shim that host-prelifts
the resolved per-volume plan + passphrase and Invokes the plugin. The deploy-model (`ResolvedBindMount`/
`ResolveVolumeBacking`), the loader (`loadEncryptedVolume`), the credential resolution, and the pure
path/state probes STAY core (consumed synchronously by non-enc deploy code).

- `internals/skills/go/SKILL.md`: the `enc.go` source-map row now describes the shim + deploy-model split and
  points the gocryptfs shelling to `candy/plugin-enc`.
- `automation/skills/enc/SKILL.md`: Source-files section split (shim vs plugin) + a Scope-Unit Architecture
  note; documents the `user_allow_other` (/etc/fuse.conf) host prerequisite for gocryptfs `-allow_other`.

### 2026-07-01 — docs(clean, settings, layer): `charly clean`/`settings`/`candy` externalized to candy/plugin-* (rest of C15)

Paired with the superproject's rest-of-C15 cutover. Three of the remaining WELDED machinery commands are now
out-of-tree `command:{clean,settings,candy}` plugins (the sixth-through-eighth welded-command externalizations,
after tmux/preempt/feature/vm/doctor); each Run handler stays core and is re-homed onto a hidden
`charly __<word>`, which the thin plugin raw-forwards to (kong passthrough covers the settings/candy command
trees, not just the clean leaf).

**`charly version` was EXCLUDED from C15 — externalizing it breaks `pkgver()` (the build stamps the package
version via `bin/charly version`); version stays a core command (RDD-vindicated).** So `core/skills/charly-version/SKILL.md`
was left unchanged (no externalization note).

- `core/skills/clean/SKILL.md`, `build/skills/settings/SKILL.md`: Overview note (external command plugin,
  raw-forward to `__<word>`, why the Run handler stays core) + the Source line names `candy/plugin-<word>/` +
  the `__<word>` re-homing.
- `image/skills/layer/SKILL.md`: the "Editing charly.yml via the CLI" section notes the TOP-LEVEL
  `charly candy` authoring tree is now the external `candy/plugin-candy` (NOT `charly new candy`).
- Usage references are unchanged — the user-facing commands are unchanged.

Paired with the superproject's C15 doctor cutover. `charly doctor` is now an out-of-tree `command:doctor`
plugin (the fifth welded-command externalization); `DoctorCmd.Run` stays core (host-detection symbols) and
is re-homed onto the hidden `charly __doctor`, which the thin plugin raw-forwards to.

- `charly-doctor/SKILL.md`: Overview note (external command plugin, raw-forward to `__doctor`, why the Run
  handler stays core); the Source line names `candy/plugin-doctor/` + the `__doctor` re-homing. Usage
  references (`run charly doctor to …`) are unchanged — the user-facing command is unchanged.

### 2026-07-01 — docs(go): RETRACT C13b (migrate as a bootstrap-phase auto-migrate plugin) as unsound

R5 claim-keyed sweep paired with the superproject's C13b retraction. RDD on the live code showed that making
`plugin-migrate` a bootstrap-phase plugin (serving `OpBootstrap` to auto-migrate a stale config's root bytes
in-memory at load) is architecturally unsound: the migrate chain is whole-project file-based + host-coupled and
needs host-prelifted inputs from a *completed* `LoadUnified`, but `runBootstrapPhase` fires *inside*
`LoadUnified` on root bytes only (a re-entry cycle; a partial pass would stamp HEAD on an incompletely-migrated
doc). So C13/M15 is COMPLETE as landed (migrate is `verb:migrate` over `OpRun`; the load gate keeps the explicit
`Run: charly migrate` reject), and the once-planned C13b is retracted.

- `/charly-internals:go` (go SKILL.md): the F9-bootstrap-phase paragraph no longer names migrate (or "M15") as
  a bootstrap plugin — it states that today only the no-op `candy/plugin-example-bootstrap` registers in the
  phase, and that migrate is `verb:migrate`/`OpRun` (the chain can't run on root bytes inside `LoadUnified`).
- The superproject sweep (its 2026-07 retraction entry) corrects the matching `.go` comments + CHANGELOG
  forward-claims; the 2026-06 plugins F9 entry above is annotated superseded.

### 2026-07-01 — docs(migrate, go, cutover-policy): migrate chain externalized to candy/plugin-migrate (TRACK 2 / M15)

The schema-migration chain moved out of charly core into the compiled-in `candy/plugin-migrate`
(`verb:migrate`/`OpRun`); `charly/migrate.go` is now a thin shim that host-prelifts the loader-coupled inputs +
Invokes the plugin. `/charly-build:migrate` skill: the registry table extended to HEAD (`2026.172.0006` →
`2026.174.1100` — matching-to-plugin / goss-verbs-to-plugin / state-provision-verbs-to-plugin /
drop-validate-ai-artifacts) + the relocation/architecture (the chain lives in the candy; the in-core shim;
host-prelift for the 5 loader-coupled migrators; `RejectLegacyPluralKeys` stays in core). go +
cutover-policy skills: the stale `charly/migrate_registry.go` references repointed. Docs-only.
