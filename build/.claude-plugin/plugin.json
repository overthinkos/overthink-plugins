{
  "name": "charly-build",
  "description": "Build/authoring CLI verbs — `charly box build`, `generate`, `list`, `inspect`, `merge`, `new`, `pull`, `validate`, plus secrets/settings/migrate/charly-mcp-cmd. Schema authoring (`image`, `layer`, `local-spec`) moved to `kind/*` plugins; eval orchestrator moved to `eval/`.",
  "version": "1.0.1",
  "author": { "name": "overthinkos" },
  "repository": "https://github.com/overthinkos/overthink-plugins"
}
